// Display consensus odds across books
interface ConsensusOddsProps {
  market: string;
  player: string;
  line: number;
}

export const ConsensusOdds: React.FC<ConsensusOddsProps> = ({ market, player, line }) => {
  // Show odds from multiple books
  // Highlight best odds
  // Display consensus line movement
}
