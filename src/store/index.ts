// Placeholder store - disabled for now
export const store = {
  dispatch: () => {},
  getState: () => ({}),
  subscribe: () => () => {},
};

export type RootState = any;
export type AppDispatch = any;
