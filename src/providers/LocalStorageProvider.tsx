// src/providers/LocalStorageProvider.tsx
import React from 'react';

export const LocalStorageProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};

export default LocalStorageProvider;
