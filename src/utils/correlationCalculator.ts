// Statistical correlation between bets
export const calculatePearsonCorrelation = (history1: number[], history2: number[]): number => {}
export const getHistoricalCorrelation = (player1: string, player2: string, market: string): number => {}
export const validateParlayLegs = (legs: any[]): { valid: boolean; reason?: string } => {}
