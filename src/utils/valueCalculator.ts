// Calculate bet value and edge
export const calculateValueScore = (odds: number, projectedProbability: number): number => {}
export const calculateKellyStake = (odds: number, probability: number, bankroll: number): number => {}
export const calculateExpectedValue = (odds: number, probability: number): number => {}
export const calculateSharpeRatio = (expectedValue: number, variance: number): number => {}
